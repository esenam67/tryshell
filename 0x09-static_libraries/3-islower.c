#include "main.h"
/**
 * _islower -check the code
 * @c: is checked
 * Description using main function
 * the program checks for lower case character
 * Return: 0 or 1
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
