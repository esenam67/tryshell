#include "main.h"
/**
 * mul - a function hat muliplies two integers
 * @a: first input
 * @b: second input
 * Return: multiplicaion result of the 2 inputs
 */
int mul(int a, int b)
{
return (a * b);
}
