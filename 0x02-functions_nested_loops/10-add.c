#include "main.h"

/**
 * add - adds two integers
 * @first: int
 * @second: int
 * Description: a function that adds two integers and
 * returns the result.
 *
 * Return: a function that adds two integers
 */

int add(int first, int second)
{
int final;
final = first + second;
return (final);
}
