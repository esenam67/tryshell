#include "main.h"
#include <stdio.h>

/**
 * _puts - check function
 * @str: pointer to a char
 * Description: a function that prints a string, followed by a
 * new line, to stdout.
 *
 * Return: nothing
 */

void _puts(char *str)
{
puts(str);
}
